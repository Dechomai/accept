{
  "name": "accept-pay",
  "version": "0.0.0",
  "main": "app.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "start": "nodemon app.js",
    "build:client": "rimraf ./public/* && cd ./client && yarn build && ncp ./build ../public",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "eslint --fix",
      "git add"
    ]
  },
  "dependencies": {
    "convict": "^4.1.0",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "mongoose": "^5.0.10"
  },
  "devDependencies": {
    "eslint": "^4.19.0",
    "eslint-plugin-react": "^7.7.0",
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "ncp": "^2.0.0",
    "nodemon": "^1.17.2",
    "prettier": "^1.11.1",
    "rimraf": "^2.6.2"
  }
}
