// Add Jasmine helpers

/* beforeEach(function() {
  jasmine.addMatchers({
    toBeCustomMatcher: function() {
      return {
        compare: function(actual, expected) {
          // string equals to expected result
          // and is equal to 'custom'
          return {
            pass: actual === expected && actual === 'custom'
          };
        }
      };
    }
  });
}); */
